# Grafana — Auto-provision Prometheus datasource
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://LogSentinel-prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "10s"
